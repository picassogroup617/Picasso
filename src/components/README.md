# Components

- `ui/` — primitive design-system components (Button, Input, Modal, etc.).
- `public/` — components used on the public site (Hero, CategoryCarousel, ContactCard, MapEmbed, QuoteModal, Footer, NavBar).
- `admin/` — components used on the Picassoadd panel (DataTable, forms, sidebars).
